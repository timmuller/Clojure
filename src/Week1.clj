(ns Week1)

; Vraag 2
; Input  : (map inc [1 2 3 4])
; Output : (2 3 4 5)

; Vraag 3
; (/ 3 4.0)

; Vraag 4
; (+ (- 465 (/ (* 774 3) 3)) 774)

; Vraag 5
; java.lang.Date vervangen voor java.util.Date
; (if (= d1 d2)
;  "de date zijn gelijk"
;  "de date zijn ongelijk"
;  )

; Vraag 6
; Cond = cond is hetzelfde als een switch in java. 
;  Er komt een bepaalde waarde in de cond en die gaat via een conditie op die waarde bepalen wat de vervolg acties zijn.
; (cond
;   (= 1 2) "een is gelijk aan twee"
;   (< 1 2) "een is kleine dan twee"
;   :else   "geen van beide"
; )

; Vraag 7
; (do (cond
;   (= 1 2) "een is gelijk aan twee"
;   (< 1 2) "een is kleine dan twee"
;   :else   "geen van beide"
; ))

; Vraag 8
; [x 8 y 1 z 2]

; Vraag 9
; (defn hello [name]
;   (str "hello " name)
; )

; Vraag 10
; [z 1 y 3 x 7]

